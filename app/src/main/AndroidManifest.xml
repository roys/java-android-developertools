<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.roysolberg.android.developertools">

    <uses-sdk tools:overrideLibrary="android.support.v4, android.support.v7.appcompat, android.support.v7.recyclerview, android.support.design" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ToolListActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ToolDetailActivity"
            android:label="@string/title_tool_detail"
            android:parentActivityName=".ToolListActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ToolListActivity" />
        </activity>
    </application>

</manifest>
